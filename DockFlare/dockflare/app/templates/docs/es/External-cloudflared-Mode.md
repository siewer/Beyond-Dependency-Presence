# Modo externo `cloudflared`

DockFlare proporciona un "Modo `cloudflared` externo" avanzado para los usuarios que desean administrar el agente `cloudflared` ellos mismos.

Este tema se trata en detalle en la página **[Interno vs. Externo `cloudflared`](Internal-vs-External-cloudflared.md)**.

Consulte esa página para obtener una explicación completa de:
* La diferencia entre los modos Interno y Externo.
* Los pros y los contras de cada enfoque.
* Cómo configurar DockFlare para usar un agente externo `cloudflared`.
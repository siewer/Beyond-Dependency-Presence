# 外部 `cloudflared` 模式

DockFlare 为想要自行管理 `cloudflared` 代理的用户提供了高级的“外部 `cloudflared` 模式”。

**[内部与外部 `cloudflared`](Internal-vs-External-cloudflared.md)** 页面详细介绍了此主题。

请参阅该页面以获取以下内容的完整说明：
* 内部模式和外部模式的区别。
* 每种方法的优缺点。
* 如何配置 DockFlare 以使用外部 `cloudflared` 代理。

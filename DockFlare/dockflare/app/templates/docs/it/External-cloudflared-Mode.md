# Modalità `cloudflared` esterna

DockFlare fornisce una "modalità `cloudflared` esterna" avanzata per gli utenti che desiderano gestire da soli l'agente `cloudflared`.

Questo argomento è trattato in dettaglio nella pagina **[Interno ed esterno `cloudflared`](Internal-vs-External-cloudflared.md)**.

Fare riferimento a quella pagina per una spiegazione completa di:
* La differenza tra le modalità Interna ed Esterna.
* I pro e i contro di ciascun approccio.
* Come configurare DockFlare per utilizzare un agente `cloudflared` esterno.